<div class="text-center course-container">
    <p>
        <button class="btn btn-primary">Add Course</button>
    </p>
    <p>
        <button class="btn btn-primary">English</button>
    </p>
    <p>
        <button class="btn btn-primary">Science</button>
    </p>
    <p>
        <button class="btn btn-primary">History</button>
    </p>
    {{!-- {{#each course}}
    <p>
        <button class="btn btn-primary">{{course.name}}</button>
    </p>
    {{/each}} --}}

</div>

<div class="course-summary">
    <h2>
        Course summary
    </h2>
    {{!--
    {{#if course}} --}}
    <p>
        {{!-- {{course.course_name}} --}}
        History
    </p>
    <p>
        {{!-- {{course.description}} --}}
        Learning events from history.
    </p>
    <p>
        <button class="btn btn-primary">Go to Course</button>
    </p>
    {{!-- {{/if }} --}}




</div>